package ru.polskiy.feedbackservice.model.type;

/**
 * Enum representing different statuses that can be associated with a complaint or feedback.
 */
public enum Status {
    RESOLVED,
    IN_PROGRESS,
    NEW,
    CLOSED
}
