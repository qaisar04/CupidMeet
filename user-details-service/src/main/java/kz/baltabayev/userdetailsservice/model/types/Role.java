package kz.baltabayev.userdetailsservice.model.types;

/**
 * Defines the roles available in the system.
 * Each user is assigned a role that determines their level of access and permissions.
 */
public enum Role {
    USER,
    ADMIN
}